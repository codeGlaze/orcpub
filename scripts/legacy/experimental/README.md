This folder contains experimental or archived helper scripts that were superseded by the `./menu` hub and `scripts/start.sh`/`scripts/stop.sh` commands.

List of archived scripts:
- dev-verify.sh — verification wrapper (archived)

If you need a script restored, move it back to `scripts/` or open a PR.
